# 项目架构设计

## 模块介绍

- **平台接口（Platform Interface）**：平台接口是项目连接其他消息渠道的接口。它通过一套标准的方式来对接到各种社交媒体系统。
  - **事件包装器接口（Event Wrapper Interface）**：事件包装器接口是将平台事件包装为标准事件的接口。它隶属于平台接口的一部分，由扩展实现。
  - **智能体行为接口（Agent Behavior Interface）**：智能体行为接口是将平台功能列举为可调用方法的接口，它隶属于平台接口的一部分，由扩展实现，同时需要提供一套标准的操作文档，以 SKILL 的方式注入到可使用该接口的 Agent 中，由 Agent 调用使用。
    - **操作接口（Action Interface）**：由智能体调用后返回操作成功与否的接口。可在操作失败时返回一定的错误信息供模型做自我修正。
    - **资源接口（Resource Interface）**：由智能体调用后返回资源的接口。在含有大量资源时应当提供分页功能。
- **权限管理系统（Permission Management System）**：权限管理系统是所有消息输入的必经之路。它有一套完善的用户组、用户、权限节点系统，能够对所有社交平台的消息进行完善的来源鉴定和授权限制，使授权归属于事件，以降低提示词注入攻击的可能性。
- **事件总线（Event Bus）**：事件总线是整个项目的核心模块、一等公民。它主要从各个模块中接收事件，并通过事件派遣机制消费。
- **事件路由（Event Router）**：事件路由从总线中提取事件并路由到合适的 Agent。它可由主 Agent 控制，并通过智能授权的方式使得某些消息可不再经过主 Agent 而直接流向子 Agent，实现消息数据分流调度，在 Agent 接收到大量任务或消息的时候非常有用。
- **智能体集群（Agent Cluster）**：智能体集群是一个基于上游 Claude Agent SDK 的核心任务处理集群。它由几个核心 Agent 组成初始状态，并在任务压力过大的时候自主分工组织，任务完成后自主销毁子 Agent。
- **行为记录器（Behavior Logger）**：行为记录器主要记录整个程序系统出现的各种行为。它可直接被所有模块横向通信以实现完备的行为记录、日志记录。
- **用户界面（User Interface）**：用户界面是项目的总体浏览工具。它作为项目扩展，主要接收由事件总线、事件路由和行为记录器产生的内部内容变化，并将其渲染到对应的页面中。

## 模块工作机制示例

### 场景一：管理员在 Telegram 上发送“查询重庆天气”

1. Telegram 平台扩展接收到消息事件，在 `平台接口 > 事件包装器接口` 层被包装为标准事件格式；
2. 事件被派遣到 `权限管理系统`，调取配置中该用户的权限节点集合，将权限注入事件对象，对事件签名；
3. 签名事件被加入 `事件总线`，`事件总线` 持续按照一定优先级调整事件队列；
4. `事件路由` 提取出该消息事件，检查会话 ID 结果为新，故路由到 `智能体集群` 的主 Agent；
5. 主 Agent 被事件中的权限对应的工具授权，读取用户消息内容，发生如下示例行为：
   1. 读取到上下文中的相关工具和接口；
   2. 使用 Telegram 平台消息回复接口做出回复：“好的，让我为你查询x月x日的重庆天气。”，同时被 `行为记录器` 记录到接口调用行为；
   3. 调用 Web-Search 工具搜索：“天气 重庆 x月x日”，同时被 `行为记录器` 记录到工具调用行为；
   4. 工具返回结果：“重庆x月x日，天气晴朗”；
   5. 使用 Telegram 平台消息回复接口：“重庆x月x日天气晴朗……”，同时被 `行为记录器` 记录到接口调用行为；
6. 上述 Agent 工作过程中，`事件总线`、`事件路由` 和 `行为记录器` 将其记录到的内容变动分发到 `用户界面`，由用户界面渲染内容变动。