# core 核心模块的事件系统和消息模型架构解释

## 事件系统大致架构解释

事件系统集成了事件系统的核心功能。

### 开发视角的事件注册机制

- 事件可能在程序的各个阶段产生。
- 开发者可以通过实现 EventListener 接口来对事件进行监听和处理。
- 事件处理器（EventHandler）大部分存在于 EventListener 中，负责处理事件、执行逻辑。
- 事件处理器也可以匿名内部类或 lambda Consumer 形式存在于事件注册调用的位置。
- 事件处理器具有优先级属性，事件会按照处理器的优先级顺序处理。

## 消息在程序中的架构解释

消息队列为本项目（kernel-claude）的核心组件、一等公民，由 core 模块定义和实现。

### 运行时的数据流转叙述

- 消息可能由其他模块产生，通过 MessageManager 传入。
- 消息存在优先级属性，以优先级队列的形式存储，且具有后期扩展到如 RabbitMQ 等外置消息队列管理工具的潜力。
- 消息队列位于权限模块之后，所有已进入消息队列的消息都已通过权限检查并携带权限细节信息。
- 所有消息会流入一个单例的、来自 agents 模块的 MessageRouter 进行路由。
